services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgresql+psycopg://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}
<<<<<<< HEAD
    depends_on:
      db:
        condition: service_healthy
=======
      # OLLAMA_BASE_URL: http://ollama:11434
    depends_on:
      db:
        condition: service_healthy
      # ollama-init:
      #   condition: service_completed_successfully
>>>>>>> 73868cecac99b697e13d5d321925ae1de3f52e78
    ports:
      - "8000:8000"
    volumes:
      - repos_data:/repos
      - .:/app
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

  db:
    image: pgvector/pgvector:pg17
    container_name: postgres
    env_file: .env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql/init.sql:/docker-entrypoint-initdb.d/01-init.sql

<<<<<<< HEAD
=======
  # ollama:
  #   image: alpine/ollama
  #   container_name: ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   mem_limit: 2g
  #   environment:
  #     - OLLAMA_MAX_LOADED_MODELS=1
  #     - OLLAMA_NUM_PARALLEL=1
  #     - OLLAMA_KEEP_ALIVE=5m
  #   healthcheck:
  #     test: ["CMD", "ollama", "list"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 10

  # ollama-init:
  #   image: alpine/ollama
  #   depends_on:
  #     ollama:
  #       condition: service_healthy
  #   entrypoint: sh -c "OLLAMA_HOST=http://ollama:11434 ollama pull unclemusclez/jina-embeddings-v2-base-code:q4"
  #   restart: "no"

>>>>>>> 73868cecac99b697e13d5d321925ae1de3f52e78
  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=https://trellis-production-183f.up.railway.app
    depends_on:
      - app
    volumes:
      - ./Frontend:/app
      - /app/node_modules
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

volumes:
  pgdata:
<<<<<<< HEAD
  repos_data:
=======
  # ollama_data:
  repos_data:
>>>>>>> 73868cecac99b697e13d5d321925ae1de3f52e78
